# Recipe Remix

React + TypeScript pantry-to-recipe app scaffolded from `context.md`.

## Run

- `npm install`
- `npm run dev`
- `npm run build`

Uses `HashRouter` so it can be hosted on GitHub Pages with root-level `index.html`.